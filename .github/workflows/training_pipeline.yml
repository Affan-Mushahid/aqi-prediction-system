name: Training pipeline

on:
  schedule:
    # Run once per day at 00:17 UTC
    - cron: '17 0 * * *'
  workflow_dispatch:

concurrency:
  group: training-pipeline
  cancel-in-progress: false

jobs:
  run-training-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f src/pipelines/training_requirements.txt ]; then pip install -r src/pipelines/training_requirements.txt; fi

      - name: Run training pipeline
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}
          DB_NAME: ${{ secrets.DB_NAME }}
        run: |
          python -u src/pipelines/training_pipeline.py